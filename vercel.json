{
    "framework": "nextjs",
    "installCommand": "node -v && corepack enable && corepack prepare pnpm@8.15.0 --activate && pnpm -v && pnpm install",
    "buildCommand": "npx prisma@^5.10.0 generate --schema=packages/db/prisma/schema.prisma && turbo run build --filter=web",
    "outputDirectory": "apps/web/.next"
}